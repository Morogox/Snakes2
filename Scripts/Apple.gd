extends Node2D

#@export var grid_size := 32
#@export var world_size := Vector2(1024, 1024) # match WorldBoundaries

#func randomize_position():         # This is useless for now, main does it for them
	#var grid_x = randi_range(0, int(world_size.x / grid_size) - 1)
	#var grid_y = randi_range(0, int(world_size.y / grid_size) - 1)
	#position = Vector2(grid_x, grid_y) * grid_size
	
